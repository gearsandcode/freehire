<!-- Never rendered: the paired +page.server.ts `load` always throws a 301 redirect
     to the homepage feed before this component mounts. SvelteKit still requires a
     +page.svelte for the route to resolve, so this is a deliberate placeholder. -->
